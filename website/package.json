{
  "name": "gitomi-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun src/build.js",
    "deploy": "./node_modules/.bin/wrangler pages deploy dist --project-name gitomi --branch main",
    "dev": "bun --hot src/server.js",
    "serve": "bun src/server.js",
    "test": "bun test"
  },
  "devDependencies": {
    "wrangler": "4.93.0"
  }
}
